{#
/**
 * @file
 */
#}

{%
  include '@component/tabs/tabs.twig' with {
    'attributes': attributes|without('class'),
    'drupal_class': attributes.class,
    'children': content.children,
  } only
%}
