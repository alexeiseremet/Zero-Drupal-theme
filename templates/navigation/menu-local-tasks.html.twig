{#
/**
 * @file
 */
#}

{% if primary %}
  <h2 class="visually-hidden">{{ 'Primary tabs'|t }}</h2>
  <ul class="inline-list">{{ primary }}</ul>
{% endif %}

{% if secondary %}
  <h2 class="visually-hidden">{{ 'Secondary tabs'|t }}</h2>
  <ul class="inline-list inline-list--secondary">{{ secondary }}</ul>
{% endif %}
