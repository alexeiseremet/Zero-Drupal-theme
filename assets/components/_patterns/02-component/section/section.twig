{#
/**
 * @file
 * Section component.
 *
 * Available variables:
 * - attributes: raw.
 * - modifier_class: string.
 * - container_class: string.
 * - content: raw.
 */
#}

{% spaceless %}
  <div class="section js-section {{ modifier_class }}"{{ attributes }}>
    <div class="section__inner {{ 'section__inner--' ~ container_class  }}">
      {% if container_class != '0' %}
        <div class="{{ container_class }}">
          {{ content }}
        </div>
      {% else %}
        {{ content }}
      {% endif %}
    </div>
  </div>
{% endspaceless %}
