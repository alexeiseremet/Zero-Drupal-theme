/**
 * @file
 * Attach behaviors for the Wysiwyg js.
 */

/* jslint browser: true */
/* global jQuery, window, document, Modernizr, Drupal */

;(function ($) {
  'use strict';

  // Drupal Behaviors.
  Drupal.behaviors.wysiwyg = {
    attach: function (context, settings) {
      // code
    }
  };

}(jQuery));
